from pyblade import live

class {class_name}(live.Component):

    def render(self):
        return self.render_inline("""
            <div>
                {{# Component content goes here #}}
            </div>
            """,
            context={{}}
        )
